# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# leftover manual curl testing artifacts
cookies.txt
*.cookie
data/industry-csv/
data/industry-csv-datasets-full.json
data/industry-csv-oids.json
data/industry-csv-refresh-failures.json

# 2026-09-04: this repo is PUBLIC on GitHub. Business planning and legal
# documents (blueprint drafts, legal review drafts) must never be
# committed - a blanket *.docx exclusion so no future draft placed in
# this folder gets swept up by a careless `git add .`, on top of the
# two files that prompted this.
*.docx
legal-review-drafts-v4.docx
tw-leads-radar-blueprint-updated-14 (2).docx

Claude outputs/
